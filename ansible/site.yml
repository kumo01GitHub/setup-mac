---
# ansible/site.yml
# Macセットアップ用メインプレイブック

- name: Mac Setup
  hosts: localhost
  connection: local
  gather_facts: true

  roles:
    - homebrew
    - mise_init
    - macos
    - dotfiles
    - xdg_normalize
