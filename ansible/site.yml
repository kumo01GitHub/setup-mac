---
# ansible/site.yml
# Macセットアップ用メインプレイブック

- name: Mac Setup
  hosts: localhost
  connection: local
  gather_facts: true

  roles:
    - homebrew
    - vscode_extensions
    - mise_init
    - macos
    - dotfiles
    - xdg_normalize
