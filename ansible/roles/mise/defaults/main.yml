---
# ansible/roles/mise/defaults/main.yml

# Keep only required minimum in defaults.
# Optional plugins/toolchains should be added via ansible/custom.yml
# using mise_plugins_extra / mise_toolchains_extra.
mise_plugins:
  []

# Required for CocoaPods install task (uses ruby@latest in this role).
mise_toolchains:
  - ruby@latest
