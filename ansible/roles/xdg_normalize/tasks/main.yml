---
# ansible/roles/xdg_normalize/tasks/main.yml
# Normalize non-XDG directories under XDG paths

- name: Create XDG directories
  file:
    path: "{{ ansible_facts['env']['HOME'] }}/{{ item }}"
    state: directory
    mode: '0755'
  loop:
    - .config
    - .cache
    - .local
    - .local/share
    - .local/state
    - .cache/ansible
    - .cache/ansible/tmp
    - .cache/Homebrew
    - .cache/mise
    - .cache/npm
    - .cache/pip
    - .cache/pub
    - .cache/python
    - .cache/python/pycache
    - .cache/gem
    - .cache/gem/specs
    - .config/docker
    - .config/mise
    - .config/npm
    - .config/pip
    - .local/state/Homebrew
    - .local/state/Homebrew/logs
    - .local/share/ansible
    - .local/share/android
    - .local/share/cocoapods
    - .local/share/gradle
    - .local/share/mise
    - .local/share/npm

- name: Move Homebrew cache under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/Library/Caches/Homebrew"
    target="{{ ansible_facts['env']['HOME'] }}/.cache/Homebrew"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_homebrew_cache
  changed_when: "'migrated' in migrate_homebrew_cache.stdout or 'removed_legacy_symlink' in migrate_homebrew_cache.stdout"

- name: Move Ansible home directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.ansible"
    target="{{ ansible_facts['env']['HOME'] }}/.local/share/ansible"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_ansible_home
  changed_when: "'migrated' in migrate_ansible_home.stdout or 'removed_legacy_symlink' in migrate_ansible_home.stdout"

- name: Move Android user directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.android"
    target="{{ ansible_facts['env']['HOME'] }}/.local/share/android"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_android_home
  changed_when: "'migrated' in migrate_android_home.stdout or 'removed_legacy_symlink' in migrate_android_home.stdout"

- name: Move Gradle home directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.gradle"
    target="{{ ansible_facts['env']['HOME'] }}/.local/share/gradle"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_gradle_home
  changed_when: "'migrated' in migrate_gradle_home.stdout or 'removed_legacy_symlink' in migrate_gradle_home.stdout"

- name: Move Docker config directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.docker"
    target="{{ ansible_facts['env']['HOME'] }}/.config/docker"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_docker_config
  changed_when: "'migrated' in migrate_docker_config.stdout or 'removed_legacy_symlink' in migrate_docker_config.stdout"

- name: Move mise home directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.mise"
    target="{{ ansible_facts['env']['HOME'] }}/.local/share/mise"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_mise_home
  changed_when: "'migrated' in migrate_mise_home.stdout or 'removed_legacy_symlink' in migrate_mise_home.stdout"

- name: Move hadolint config file under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.hadolint.yaml"
    target="{{ ansible_facts['env']['HOME'] }}/.config/hadolint.yaml"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -f "$legacy" ]; then
      cp -a "$legacy" "$target"
      rm -f "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_hadolint_config
  changed_when: "'migrated' in migrate_hadolint_config.stdout or 'removed_legacy_symlink' in migrate_hadolint_config.stdout"

- name: Move npm cache directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.npm"
    target="{{ ansible_facts['env']['HOME'] }}/.cache/npm"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_npm_cache
  changed_when: "'migrated' in migrate_npm_cache.stdout or 'removed_legacy_symlink' in migrate_npm_cache.stdout"

- name: Move Flutter pub cache directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.pub-cache"
    target="{{ ansible_facts['env']['HOME'] }}/.cache/pub"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_flutter_pub_cache
  changed_when: "'migrated' in migrate_flutter_pub_cache.stdout or 'removed_legacy_symlink' in migrate_flutter_pub_cache.stdout"

- name: Move npm config file under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.npmrc"
    target="{{ ansible_facts['env']['HOME'] }}/.config/npm/npmrc"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -f "$legacy" ]; then
      cp -a "$legacy" "$target"
      rm -f "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_npmrc
  changed_when: "'migrated' in migrate_npmrc.stdout or 'removed_legacy_symlink' in migrate_npmrc.stdout"

- name: Move pip legacy directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.pip"
    target="{{ ansible_facts['env']['HOME'] }}/.config/pip"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_pip_legacy
  changed_when: "'migrated' in migrate_pip_legacy.stdout or 'removed_legacy_symlink' in migrate_pip_legacy.stdout"

- name: Move pip cache directory under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/Library/Caches/pip"
    target="{{ ansible_facts['env']['HOME'] }}/.cache/pip"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_pip_cache
  changed_when: "'migrated' in migrate_pip_cache.stdout or 'removed_legacy_symlink' in migrate_pip_cache.stdout"

- name: Move RubyGems cache under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.gem"
    target="{{ ansible_facts['env']['HOME'] }}/.cache/gem"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_gem_cache
  changed_when: "'migrated' in migrate_gem_cache.stdout or 'removed_legacy_symlink' in migrate_gem_cache.stdout"

- name: Move CocoaPods home under XDG
  shell: |
    set -eu
    legacy="{{ ansible_facts['env']['HOME'] }}/.cocoapods"
    target="{{ ansible_facts['env']['HOME'] }}/.local/share/cocoapods"

    if [ -L "$legacy" ]; then
      rm -f "$legacy"
      echo removed_legacy_symlink
    elif [ -d "$legacy" ]; then
      if [ -n "$(ls -A "$legacy" 2>/dev/null)" ]; then
        cp -a "$legacy"/. "$target"/
      fi
      rm -rf "$legacy"
      echo migrated
    fi
  args:
    executable: /bin/bash
  register: migrate_cocoapods_home
  changed_when: "'migrated' in migrate_cocoapods_home.stdout or 'removed_legacy_symlink' in migrate_cocoapods_home.stdout"
